# Custom key icons

Drop SVG files here named after the Lucide icon they replace (e.g. `zap.svg`,
`circle-check.svg`). They override the bundled Lucide icons for the matching
keycaps. See `src/keycaps.js` for the icon names each key uses.
